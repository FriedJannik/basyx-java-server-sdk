/*
 * This file was generated by the Gradle 'init' task.
 */

plugins {
    id("buildlogic.java-conventions")
}

dependencies {
    api(project(":basyx.mongodbcore"))
    api(project(":basyx.aasrepository-core"))
    api(project(":basyx.aasrepository-backend"))
    api(project(":basyx.aasservice-core"))
    api(project(":basyx.aasservice-backend-inmemory"))
    api(libs.org.springframework.spring.context)
    api(libs.org.springframework.boot.spring.boot.starter.data.mongodb)
    api(libs.commons.io.commons.io)
    api(project(":basyx.filerepository-backend-mongodb"))
    testImplementation(project( path =":basyx.aasrepository-core", configuration = "testArtifacts"))
    testImplementation(project( path =":basyx.aasservice-core", configuration = "testArtifacts"))
}

description = "BaSyx AAS Repository backend-mongodb"

val testsJar by tasks.registering(Jar::class) {
    archiveClassifier = "tests"
    from(sourceSets["test"].output)
}
configurations {
    create("testArtifacts") {
        extendsFrom(testRuntimeClasspath.get())
    }
}
artifacts {
    add("testArtifacts", testsJar)
}

(publishing.publications["maven"] as MavenPublication).artifact(testsJar)

java {
    withJavadocJar()
}
