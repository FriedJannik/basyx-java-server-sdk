/*
 * This file was generated by the Gradle 'init' task.
 */

plugins {
    id("buildlogic.java-conventions")
}

dependencies {
    api(project(":basyx.aasservice-core"))
    api(libs.org.springframework.boot.spring.boot.starter)
    api(project(":basyx.filerepository-backend-inmemory"))
    testImplementation(project( path =":basyx.aasservice-core", configuration = "testArtifacts"))
}

description = "BaSyx AAS Service backend-inmemory"

val testsJar by tasks.registering(Jar::class) {
    archiveClassifier = "tests"
    from(sourceSets["test"].output)
}
configurations {
    create("testArtifacts") {
        extendsFrom(testRuntimeClasspath.get())
    }
}
artifacts {
    add("testArtifacts", testsJar)
}

(publishing.publications["maven"] as MavenPublication).artifact(testsJar)

java {
    withJavadocJar()
}
