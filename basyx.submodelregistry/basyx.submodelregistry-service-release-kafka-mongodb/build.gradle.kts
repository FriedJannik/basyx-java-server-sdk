/*
 * This file was generated by the Gradle 'init' task.
 */

plugins {
    id("buildlogic.java-conventions")
}

dependencies {
    api(project(":basyx.submodelregistry-feature-authorization"))
    api(project(":basyx.authorization"))
    api(project(":basyx.submodelregistry-service-mongodb-storage"))
    api(libs.org.apache.commons.commons.lang3)
    api(project(":basyx.submodelregistry-service-kafka-events"))
    testImplementation(libs.org.junit.vintage.junit.vintage.engine)
    testImplementation(libs.org.springframework.boot.spring.boot.starter.test)
    testImplementation(project( path =":basyx.submodelregistry-service-basetests", configuration = "testArtifacts"))
}

description = "BaSyx Submodel Registry Service Release Kafka MongoDB"

val testsJar by tasks.registering(Jar::class) {
    archiveClassifier = "tests"
    from(sourceSets["test"].output)
}
configurations {
    create("testArtifacts") {
        extendsFrom(testRuntimeClasspath.get())
    }
}
artifacts {
    add("testArtifacts", testsJar)
}

(publishing.publications["maven"] as MavenPublication).artifact(testsJar)

java {
    withJavadocJar()
}
